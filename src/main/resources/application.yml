server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    root: INFO
    com.kpmg.qtracker: INFO

spring:
  flyway:
    baseline-on-migrate: true
    baseline-version: 0
  web:
    resources:
      static-locations: classpath:/static/
  mail:
    host: ${SPRING_MAIL_HOST:goemairs.go.kworld.kpmg.com}
    port: ${SPRING_MAIL_PORT:25}
    username: ${SPRING_MAIL_USERNAME:}
    password: ${SPRING_MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: true
            required: true
          connectiontimeout: 30000
          timeout: 30000
          writetimeout: 30000

app:
  base-url: ${APP_BASE_URL:}

reminders:
  enabled: true

overdue:
  use-working-days: ${OVERDUE_USE_WORKING_DAYS:true}

notifications:
  email:
    enabled: ${NOTIFICATIONS_EMAIL_ENABLED:false}
    from: ${NOTIFICATIONS_EMAIL_FROM:sseytbekov@kpmg.kz}
    whitelist-enforced: ${NOTIFICATIONS_EMAIL_WHITELIST_ENFORCED:false}
    whitelist: ${NOTIFICATIONS_EMAIL_WHITELIST:}

controls:
  auto-create:
    enabled: ${CONTROLS_AUTO_CREATE_ENABLED:true}
    